services:
  langgraph-gateway:
    build: .
    container_name: langgraph-gateway
    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "127.0.0.1:8000:8000"

    networks:
      - ainet

networks:
  ainet:
    external: true
